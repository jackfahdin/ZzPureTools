状态: 未执行
测试日期:
测试人员:
OS/版本:
Qt/工具链:
设备/显示器:
构建产物摘要:
结果:
问题链接:

# macOS 真机验收清单

本清单至少在 macOS 13.3+ 最低兼容环境和当前发布版 macOS 执行。arm64 与 x86_64 使用各自架构匹配的 Qt SDK 和产物；Rosetta 只能用于运行验证，不能代替 x86_64 Qt SDK 构建证据。开始前保存 `build/gate-evidence/macos-native.log` 和每个产物的 `lipo` 结果。

| 检查项 | 预期行为 | 实际结果 | 截图/日志路径 | 问题链接 |
|---|---|---|---|---|
| [ ] macOS 13.3+ / arm64 shared | 原生启动、退出和基础导航正常，产物只有 arm64 架构 |  |  |  |
| [ ] macOS 13.3+ / arm64 static | 原生启动、退出和基础导航正常，不依赖未部署的一方 dylib |  |  |  |
| [ ] macOS 13.3+ / x86_64 shared | x86_64 启动、退出和基础导航正常，产物只有 x86_64 架构 |  |  |  |
| [ ] macOS 13.3+ / x86_64 static | x86_64 启动、退出和基础导航正常，不依赖未部署的一方 dylib |  |  |  |
| [ ] 当前发布版 macOS | 四个架构/链接组合在当前系统完成基础运行 |  |  |  |
| [ ] Retina | 标题栏、文字、图标和 resize 命中区域清晰且尺寸正确 |  |  |  |
| [ ] 多显示器 | 跨 Retina/非 Retina 显示器移动后 DPR、几何和绘制及时更新 |  |  |  |
| [ ] traffic lights | 关闭、最小化、缩放按钮位置、状态、hover 和点击符合系统行为 |  |  |  |
| [ ] 标题栏拖动与双击 | 可拖动窗口，交互子控件不误拖；双击行为遵循系统设置 |  |  |  |
| [ ] 边缘与角落 resize | 所有边角可连续调整大小，cursor 和最小尺寸正确 |  |  |  |
| [ ] 全屏 | 进入和退出原生全屏、Space 切换和恢复几何正常 |  |  |  |
| [ ] Light/Dark 主题 | 系统外观切换后控件、标题栏、图标和焦点视觉同步 |  |  |  |
| [ ] blur/backdrop | 支持时效果正确；不支持时返回可解释降级且内容仍清晰 |  |  |  |
| [ ] 键盘导航 | Tab、Shift+Tab、Enter、Space、Escape 和方向键符合控件语义 |  |  |  |
| [ ] 可访问性 | VoiceOver 可读出名称、角色和状态，焦点顺序可预测 |  |  |  |
| [ ] 最小化/恢复与多窗口 | Dock 操作、多个窗口、关闭和重新打开后状态隔离正确 |  |  |  |
| [ ] 反复窗口生命周期 | 连续创建关闭 100 次后无崩溃、悬挂窗口或适配层对象残留 |  |  |  |

## 签署规则

所有目标系统、架构、链接方式和交互项都要填写实际结果与证据。无问题时“问题链接”填写“无”；存在问题时填写可访问的问题记录并标明发布影响。只有字段完整、所有适用项完成且没有未解决的发布阻断问题，才能把首行状态改为 `真机验收通过`。
