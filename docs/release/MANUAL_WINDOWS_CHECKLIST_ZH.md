状态: 未执行
测试日期:
测试人员:
OS/版本:
Qt/工具链:
设备/显示器:
构建产物摘要:
结果:
问题链接:

# Windows 真机验收清单

本清单分别用于 Windows 10 22H2 和 Windows 11。MSVC 与 Qt 官方 MinGW 包必须各自执行并记录，不能共用一次结果。开始前保存 `build/gate-evidence/windows-native.log`，并确认被测包摘要与本页“构建产物摘要”一致。

| 检查项 | 预期行为 | 实际结果 | 截图/日志路径 | 问题链接 |
|---|---|---|---|---|
| [ ] Windows 10 22H2 / MSVC shared | 应用启动、退出和基础导航正常，加载 MSVC ABI 包 |  |  |  |
| [ ] Windows 10 22H2 / MSVC static | 应用启动、退出和基础导航正常，不依赖未部署的一方 DLL |  |  |  |
| [ ] Windows 10 22H2 / MinGW shared | 应用启动、退出和基础导航正常，加载匹配的 MinGW/Qt runtime |  |  |  |
| [ ] Windows 10 22H2 / MinGW static | 应用启动、退出和基础导航正常，不混入 MSVC ABI 文件 |  |  |  |
| [ ] Windows 11 / MSVC shared | 应用启动、退出和基础导航正常，加载 MSVC ABI 包 |  |  |  |
| [ ] Windows 11 / MSVC static | 应用启动、退出和基础导航正常，不依赖未部署的一方 DLL |  |  |  |
| [ ] Windows 11 / MinGW shared | 应用启动、退出和基础导航正常，加载匹配的 MinGW/Qt runtime |  |  |  |
| [ ] Windows 11 / MinGW static | 应用启动、退出和基础导航正常，不混入 MSVC ABI 文件 |  |  |  |
| [ ] 100% DPI | 标题栏、内容、边框和命中区域尺寸正确，无裁切或重叠 |  |  |  |
| [ ] 150% DPI | 字体、图标和命中区域随 DPI 正确缩放，窗口移动后刷新正常 |  |  |  |
| [ ] 200% DPI | 高 DPI 下无裁切、模糊拉伸或错误 resize cursor |  |  |  |
| [ ] 多显示器与混合 DPI | 跨显示器移动时 DPR、窗口大小、标题栏和 hit test 同步更新 |  |  |  |
| [ ] 四边 resize | 从上、下、左、右边缘均可连续调整大小，最小尺寸有效 |  |  |  |
| [ ] 四角 resize | 四角 cursor 与双轴调整方向正确，无跳动或失去捕获 |  |  |  |
| [ ] 标题栏拖动 | 可拖动窗口；按钮、输入框和交互区域不会误触发拖动 |  |  |  |
| [ ] 双击最大化 | 标题栏双击在最大化和还原间切换，几何位置正确 |  |  |  |
| [ ] 系统菜单 | 标题栏约定位置和键盘操作可打开系统菜单，命令状态正确 |  |  |  |
| [ ] Windows 11 Snap Layout | 最大化按钮悬停和系统手势可触发 Snap Layout，不被自绘层遮挡 |  |  |  |
| [ ] 最小化与恢复 | 任务栏最小化、恢复、最大化和多次切换后状态一致 |  |  |  |
| [ ] Light/Dark 主题 | 系统与应用主题切换后背景、文字、边框、图标和标题栏同步 |  |  |  |
| [ ] 键盘导航 | Tab/Shift+Tab、Enter、Space、Escape 和方向键行为符合控件语义 |  |  |  |
| [ ] 可访问性 | Narrator 可读出控件名称、角色、状态，焦点顺序和高对比度可用 |  |  |  |
| [ ] 反复窗口生命周期 | 连续创建关闭 100 次后无崩溃、悬挂窗口或适配层对象残留 |  |  |  |

## 签署规则

所有目标 OS、ABI、DPI 和交互行都要填写实际结果与证据。无问题时“问题链接”填写“无”；存在问题时填写可访问的问题记录，并明确是否阻断发布。只有字段完整、所有适用项完成且没有未解决的发布阻断问题，才能把首行状态改为 `真机验收通过`。
